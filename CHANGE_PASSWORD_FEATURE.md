# Change Password Feature

## Overview
تم إضافة ميزة تعديل كلمة السر للمستخدمين. يمكن للمستخدم الوصول إليها من صفحة البروفايل.

## Implementation Details

### Backend Changes

#### 1. `database/db_users.js`
- **دالة جديدة**: `updatePassword(email, oldPassword, newPassword)`
  - التحقق من كلمة السر القديمة
  - تحديث كلمة السر الجديدة مع التشفير
  - حفظ التغييرات في قاعدة البيانات

#### 2. `database/ipc-handlers-users.js`
- **Handler جديد**: `users:updatePassword`
  - يستقبل: `{ email, oldPassword, newPassword }`
  - يرجع: `{ success: true/false, message/error }`

#### 3. `preload.js`
- **API جديد**: `window.electron.users.updatePassword(email, oldPassword, newPassword)`

### Frontend Changes

#### 1. `frontend/pages/profile.js`
- **زر جديد**: "Change Password" بجانب زر Logout
  - أيقونة: 🔐
  - اللون: رمادي ثانوي

- **Modal جديد**: `changePasswordModal`
  - حقل: كلمة السر القديمة
  - حقل: كلمة السر الجديدة
  - حقل: تأكيد كلمة السر الجديدة
  - زر: تحديث كلمة السر
  - زر: إلغاء

- **التحقق من البيانات**:
  - التحقق من عدم ترك أي حقل فارغ
  - التحقق من تطابق كلمات السر الجديدة
  - التحقق من طول كلمة السر (6 أحرف على الأقل)

#### 2. `frontend/styles.css`
- **أنماط Modal**:
  - خلفية شفافة
  - تأثير fadeIn
  - تأثير slideUp للمحتوى
  - ظلال وزوايا مستديرة

- **أنماط الأزرار**:
  - زر Primary (أزرق)
  - زر Secondary (رمادي)

## Usage

### للمستخدم:
1. انتقل إلى صفحة البروفايل
2. اضغط على زر "Change Password"
3. أدخل كلمة السر القديمة
4. أدخل كلمة السر الجديدة
5. أدخل تأكيد كلمة السر الجديدة
6. اضغط على "Update Password"

### الرسائل:
- ✅ نجاح: "Password updated successfully"
- ❌ خطأ: "Old password is incorrect"
- ❌ خطأ: "New passwords do not match"
- ❌ خطأ: "New password must be at least 6 characters"

## Security Features
- ✅ التحقق من كلمة السر القديمة قبل التحديث
- ✅ تشفير كلمة السر الجديدة (SHA256)
- ✅ التحقق من صحة البيانات على الواجهة الأمامية والخادم
- ✅ رسائل خطأ واضحة وآمنة

## Files Modified
1. `database/db_users.js` - إضافة دالة updatePassword
2. `database/ipc-handlers-users.js` - إضافة handler
3. `preload.js` - إضافة API
4. `frontend/pages/profile.js` - إضافة UI والمنطق
5. `frontend/styles.css` - إضافة أنماط

## Testing
للاختبار:
1. تسجيل الدخول بحساب موجود
2. الذهاب إلى صفحة البروفايل
3. اضغط على "Change Password"
4. جرب الحالات المختلفة:
   - كلمة سر قديمة خاطئة
   - عدم تطابق كلمات السر الجديدة
   - كلمة سر جديدة قصيرة جداً
   - تحديث ناجح
