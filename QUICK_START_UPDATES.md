# دليل البدء السريع - نظام التحديث التلقائي
# Quick Start Guide - Auto-Update System

## 🚀 البدء السريع

### 1️⃣ تثبيت المكتبات

```bash
npm install
```

هذا سيثبت:
- `electron-updater` - نظام التحديث التلقائي
- `electron-log` - تسجيل الأحداث

---

### 2️⃣ إعداد GitHub (للنشر المجاني)

#### أ. إنشاء مستودع GitHub

1. اذهب إلى: https://github.com/new
2. **Repository name:** `Gestion-des-Factures`
3. اختر **Private** أو **Public**
4. انقر **Create repository**

#### ب. إنشاء GitHub Token

1. اذهب إلى: https://github.com/settings/tokens
2. انقر **Generate new token (classic)**
3. اختر صلاحية **`repo`** ✅
4. انسخ الـ Token (يبدأ بـ `ghp_...`)

#### ج. إضافة الـ Token

**في PowerShell:**
```powershell
$env:GH_TOKEN="ghp_your_token_here"
```

**أو بشكل دائم (موصى به):**
1. اضغط `Win + R` → اكتب `sysdm.cpl`
2. **Advanced** → **Environment Variables**
3. **User variables** → **New**
4. **Variable name:** `GH_TOKEN`
5. **Variable value:** `ghp_your_token_here`

---

### 3️⃣ نشر أول إصدار

```powershell
# باستخدام السكريبت (سهل)
.\publish-update.ps1 -NewVersion 1.0.0 -GitHub

# أو يدوياً
npm run build:win -- --publish always
```

---

### 4️⃣ نشر تحديث جديد

```powershell
.\publish-update.ps1 -NewVersion 1.0.1 -GitHub
```

---

## 📋 كيف يعمل النظام

1. **عند تشغيل التطبيق** → يتحقق تلقائياً من التحديثات بعد 3 ثوانٍ
2. **عند توفر تحديث** → يظهر إشعار للمستخدم
3. **المستخدم يختار** → تنزيل الآن أو لاحقاً
4. **أثناء التنزيل** → يظهر شريط التقدم
5. **بعد التنزيل** → طلب إعادة التشغيل لتثبيت التحديث

---

## 🎯 الملفات المهمة

- **`updater.js`** - نظام إدارة التحديثات
- **`package.json`** - إعدادات النشر والإصدار
- **`publish-update.ps1`** - سكريبت النشر السريع
- **`main.js`** - دمج نظام التحديث
- **`preload.js`** - API للتحديثات

---

## ⚙️ التخصيص

### تغيير اللغة الافتراضية

في `updater.js`:
```javascript
let currentLanguage = 'ar'; // ar, en, أو fr
```

### تغيير وقت التحقق من التحديثات

في `updater.js`:
```javascript
setTimeout(() => {
    checkForUpdates();
}, 3000); // 3 ثوانٍ (3000 ميلي ثانية)
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: `GH_TOKEN is not set`

**الحل:**
```powershell
$env:GH_TOKEN="ghp_your_token_here"
```

### المشكلة: التحديث لا يظهر

**الحل:**
- تأكد من أن رقم الإصدار الجديد أكبر من القديم
- تحقق من وجود الملفات في GitHub Releases
- تأكد من وجود `latest.yml` في الـ Release

---

## 📚 روابط مفيدة

- [GitHub Personal Access Tokens](https://github.com/settings/tokens)
- [electron-updater Documentation](https://www.electron.build/auto-update)
- [electron-builder Publishing](https://www.electron.build/configuration/publish)

---

## ✨ الخلاصة

✅ نظام التحديث التلقائي جاهز تماماً
✅ يدعم ثلاث لغات (عربي، إنجليزي، فرنسي)
✅ سهل النشر باستخدام سكريبت PowerShell
✅ مجاني تماماً باستخدام GitHub Releases

**مبروك! 🎉 تطبيقك الآن يدعم التحديث التلقائي!**

---

تم إنشاؤه بواسطة Cascade AI - 2025
